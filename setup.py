# from setuptools import find_packages, setup

# setup(
#     name='gsort',
#     packages=find_packages(),
# )


from setuptools import find_packages#, setup
from distutils.core import setup


setup(
    name='gsort',
    packages=find_packages()
)
